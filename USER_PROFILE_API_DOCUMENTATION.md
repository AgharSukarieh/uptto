# 👤 توثيق API الصفحة الشخصية (User Profile API Documentation)

تم إنشاء هذا الملف بناءً على التوثيق المقدم من المستخدم.

## 🔗 Base URL

```
http://arabcodetest.runasp.net
```

---

## 📝 ملخص التغييرات المطبقة:

1. ✅ تم إنشاء `bellActivationService.js` مع جميع الـ endpoints المطلوبة
2. ✅ تم تحديث `UserProfile.js` لاستخدام الـ API الجديد
3. ✅ تم التأكد من أن جميع الصور الشخصية تؤدي إلى صفحة المستخدم (`/Profile/{userId}`)
4. ✅ الأزرار تعمل بشكل صحيح:
   - إذا كان المستخدم الحالي هو صاحب الصفحة: "محاولاتي" و "تعديل الملف الشخصي"
   - إذا كان المستخدم الحالي ليس صاحب الصفحة: "متابعة"/"إلغاء المتابعة" و "الإشعارات" (الجرس)

---

## 📂 الملفات المحدثة:

1. **`src/Service/bellActivationService.js`** (جديد)
   - `getBellFollowersCount(userId)` - جلب عدد المهتمين بالإشعارات
   - `getBellActivationStatus(followerId, followedId)` - جلب حالة تفعيل الإشعارات
   - `saveBellActivation(followerId, followedId, emailEnabled, appEnabled)` - حفظ/تحديث تفعيل الإشعارات

2. **`src/Pages/User/UserProfile.js`** (محدث)
   - تم تحديث الاستيرادات لاستخدام `bellActivationService`
   - تم تحديث `useEffect` لجلب حالة الجرس من الـ API الجديد
   - تم تحديث `handleBellClick` و `handleSaveBellPreferences` لاستخدام الـ API الجديد

---

## ✅ التحقق من الصور الشخصية:

جميع الصور الشخصية في الملفات التالية تؤدي إلى صفحة المستخدم:
- ✅ `AllPost.js` - جميع الصور الشخصية
- ✅ `CommentsModal.js` - جميع الصور الشخصية في التعليقات والردود
- ✅ `Post.js` - جميع الصور الشخصية في التعليقات والردود

---

## 🎯 الوظائف المطلوبة:

### 1. جلب معلومات المستخدم
- ✅ يستخدم `getUserById` من `userService.js`
- ✅ Endpoint: `GET /api/users/{userId}`

### 2. جلب إحصائيات المشاكل العامة
- ✅ يستخدم `getAllGeneralInfoUser` من `ProblemService.js`
- ✅ Endpoint: `GET /api/general/User`

### 3. جلب عدد المهتمين بالإشعارات
- ✅ يستخدم `getBellFollowersCount` من `bellActivationService.js`
- ✅ Endpoint: `GET /api/Users/{userId}/bell-activations`

### 4. جلب حالة تفعيل الإشعارات
- ✅ يستخدم `getBellActivationStatus` من `bellActivationService.js`
- ✅ Endpoint: `GET /api/bell-activations?followerId={followerId}&followedId={followedId}`

### 5. حفظ/تحديث تفعيل الإشعارات
- ✅ يستخدم `saveBellActivation` من `bellActivationService.js`
- ✅ Endpoint: `POST /api/bell-activations` أو `PUT /api/bell-activations`

### 6. متابعة مستخدم
- ✅ يستخدم `doFollow` من `followService.js`
- ✅ Endpoint: `POST /api/follows`

### 7. إلغاء متابعة مستخدم
- ✅ يستخدم `doUnfollow` من `followService.js`
- ✅ Endpoint: `DELETE /api/follows`

### 8. التحقق من حالة المتابعة
- ✅ يستخدم `checkFollowStatus` من `followService.js`
- ✅ Endpoint: `GET /api/follows/status?followerId={followerId}&followId={followId}`

---

## 📌 ملاحظات:

1. جميع الطلبات تحتاج إلى Token في Header (يتم إضافة Token تلقائياً عبر axios interceptor)
2. إذا كان المستخدم الحالي هو صاحب الصفحة، تظهر أزرار "محاولاتي" و "تعديل الملف الشخصي"
3. إذا كان المستخدم الحالي ليس صاحب الصفحة، تظهر أزرار "متابعة"/"إلغاء المتابعة" و "الإشعارات" (الجرس)
4. زر الجرس يظهر فقط إذا كان المستخدم يتابع صاحب الصفحة
5. عند الضغط على أي صورة شخصية، يتم الانتقال إلى صفحة المستخدم (`/Profile/{userId}`)

